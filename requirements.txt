# HydroSentinel Backend Dependencies
# Install with: pip install -r requirements.txt

# ==========================================
# CORE DEPENDENCIES (Required)
# ==========================================

# FastAPI & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# ==========================================
# GEOSPATIAL PROCESSING (Required for full functionality)
# ==========================================

# Geospatial Processing
numpy==1.24.3
shapely==2.0.2

# Rasterio (May require special installation on Windows)
# If installation fails, see README.md for alternative methods
rasterio==1.3.9

# ==========================================
# OPTIONAL DEPENDENCIES
# ==========================================

# For API testing
requests==2.31.0

# Note: If rasterio fails to install on Windows, try:
# 1. pip install pipwin && pipwin install rasterio
# 2. OR use conda: conda install -c conda-forge rasterio
# 3. OR download wheel from: https://www.lfd.uci.edu/~gohlke/pythonlibs/
#
# The application will work with demo/fallback data even without rasterio.

